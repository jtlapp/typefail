
import 'mocha';
import { assert } from 'chai';
import { TypeTest } from '../src';

describe("compiler configuration", () => {

    it("uses provided compiler configuration", (done) => {

        done();
    });

    it("errors on invalid compiler configuration", (done) => {

        done();
    });

    it("uses named tsconfig.json file", (done) => {

        done();
    });

    it("errors when named tsconfig.json file not found", (done) => {

        done();
    });

    it("uses nearest tsconfig.json file", (done) => {

        done();
    });

    it("errors when there is no nearest tsconfig.json file", (done) => {

        done();
    });
});
