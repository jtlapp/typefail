
/* typetest:group "Accepts all same-line errors by default" */

/* typetest:group "Accepts a single expected error" */


/* typetest:group "Accepts multiple same-line errors upon matching one" */


/* typetest:group "Accepts multiple matching same line errors" */


/* typetest:group "Fails when no error occurs" */


/* typetest:group "Fails when no matching error occurs" */


/* typetest:group "Fails when only one of several expected errors occurs" */


/* typetest:group "Checks each of differing directive syntaxes" */


// typetest:group "Recognizes groups with slash-slash syntax"

